parameters:
  dependsOn: []
  condition: 'succeeded()'
  name: ''
  pool:
    vmImage: 'ubuntu-latest'

jobs:
- job: ${{ parameters.name }}
  displayName: ${{ parameters.name }}
  timeoutInMinutes: 30
  dependsOn: ${{ parameters.dependsOn }}
  condition: ${{ parameters.condition }}
  pool: ${{ parameters.pool }}

  steps:
    - checkout: self
      fetchDepth: 0

    # - task: AzureCLI@2
    #   displayName: 'Azure Login (Workload Identity Federation)'
    #   inputs:
    #     azureSubscription: 'AzureServiceConnectionWIF'
    #     scriptType: 'bash'  
    #     scriptLocation: 'inlineScript'
    #     inlineScript: |
    #         echo "Attempting to log into Azure..."
    #         az account show > /dev/null 2>&1
    #         if [ $? -eq 0 ]; then
    #             echo "##vso[task.logissue type=warning]Already logged in, skipping re-authentication."
    #         else
    #             echo "Successfully logged into Azure via Service Connection."
    #         fi
    #   env:
    #     AZURE_CLIENT_ID: $(AZURE-APP-CLIENT-ID)
    #     AZURE_TENANT_ID: $(AZURE-TENANT-ID)
    #     AZURE_SUBSCRIPTION_ID: $(AZURE-SUBSCRIPTION-ID)

    # - task: AzureCLI@2
    #   displayName: 'ACR Login (via Azure CLI)'
    #   inputs:
    #     azureSubscription: '$(AZURE_SERVICE_CONNECTION)'
    #     scriptType: 'bash'
    #     scriptLocation: 'inlineScript'
    #     inlineScript: |
    #         az acr login --name $(AZURE_CONTAINER_REGISTRY_SHORT_NAME)
    #         echo "Logged into ACR: $(AZURE_CONTAINER_REGISTRY_SHORT_NAME)"


      # Generate Docker tags dynamically
    - script: |
          echo "--- Starting Generate Docker Image Tags Script ---"

          # Combine the ACR parts to form the full registry name
          # This should prevent Azure DevOps from masking/transforming the value
          FULL_AZURE_CONTAINER_REGISTRY="$(ACR_REGISTRY_PREFIX)$(ACR_REGISTRY_SUFFIX)"
          if [ -z "${FULL_AZURE_CONTAINER_REGISTRY}" ]; then
            echo "##vso[task.logissue type=error]FULL_AZURE_CONTAINER_REGISTRY is empty. This is critical for tagging."
            exit 1
          fi
          echo "DEBUG: FULL_AZURE_CONTAINER_REGISTRY = ${FULL_AZURE_CONTAINER_REGISTRY}"

          # Get short Git commit SHA for immutable tagging (first 7 characters for brevity)
          SHORT_SHA=$(echo "$(Build.SourceVersion)" | cut -c1-7)
          if [ -z "$SHORT_SHA" ]; then
            echo "##vso[task.logissue type=error]SHORT_SHA could not be determined from Build.SourceVersion. Exiting."
            exit 1
          fi
          echo "DEBUG: SHORT_SHA = ${SHORT_SHA}"

          # Clean branch name for use in tags (removes prefixes, converts slashes to hyphens, to lowercase, removes invalid chars)
          CLEAN_BRANCH_NAME=$(echo "$(Build.SourceBranch)" | \
              sed 's/^refs\/heads\///g' | \
              sed 's/^refs\/pull\///g' | \
              sed 's/^refs\/tags\///g' | \
              tr '/' '-' | \
              tr '[:upper:]' '[:lower:]' | \
              sed 's/[^a-z0-9\.-]//g' ) # Allow dots and hyphens in the cleaned name
          
          if [ -z "$CLEAN_BRANCH_NAME" ]; then
            CLEAN_BRANCH_NAME="unknown-branch" # Fallback if cleaning results in empty string
            echo "##vso[task.logissue type=warning]CLEAN_BRANCH_NAME is empty after sanitization, defaulting to '${CLEAN_BRANCH_NAME}'."
          fi
          echo "DEBUG: CLEAN_BRANCH_NAME = ${CLEAN_BRANCH_NAME}"

          # Construct base tag prefixes with the full registry name
          API_BASE_TAG_PREFIX="${FULL_AZURE_CONTAINER_REGISTRY}/$(API_IMAGE_NAME)"
          FRONTEND_BASE_TAG_PREFIX="${FULL_AZURE_CONTAINER_REGISTRY}/$(FRONTEND_IMAGE_NAME)"
          echo "DEBUG: API_BASE_TAG_PREFIX = ${API_BASE_TAG_PREFIX}"
          echo "DEBUG: FRONTEND_BASE_TAG_PREFIX = ${FRONTEND_BASE_TAG_PREFIX}"

          # Initialize tags with immutable Git SHA (comma-separated for Docker@2 task)
          API_TAGS="${API_BASE_TAG_PREFIX}:${SHORT_SHA}"
          FRONTEND_TAGS="${FRONTEND_BASE_TAG_PREFIX}:${SHORT_SHA}"
          echo "DEBUG: Initial API Tags (SHA): ${API_TAGS}"
          echo "DEBUG: Initial Frontend Tags (SHA): ${FRONTEND_TAGS}"

          # Add branch-specific tags for development traceability
          API_TAGS="${API_TAGS},${API_BASE_TAG_PREFIX}:${CLEAN_BRANCH_NAME}-${SHORT_SHA}"
          FRONTEND_TAGS="${FRONTEND_TAGS},${FRONTEND_BASE_TAG_PREFIX}:${CLEAN_BRANCH_NAME}-${SHORT_SHA}"
          echo "DEBUG: API Tags (with branch-SHA): ${API_TAGS}"
          echo "DEBUG: Frontend Tags (with branch-SHA): ${FRONTEND_TAGS}"

          # Add additional tags based on push type (e.g., semantic versions for releases, 'latest')
          if [[ "$(Build.SourceBranch)" == refs/tags/v* ]]; then
            VERSION_TAG=$(echo "$(Build.SourceBranch)" | sed 's/refs\/tags\/v//')
            API_TAGS="${API_TAGS},${API_BASE_TAG_PREFIX}:${VERSION_TAG}"
            FRONTEND_TAGS="${FRONTEND_TAGS},${FRONTEND_BASE_TAG_PREFIX}:${VERSION_TAG}"
            echo "Detected Git tag push, adding semantic version tag: ${VERSION_TAG}"

            API_TAGS="${API_TAGS},${API_BASE_TAG_PREFIX}:latest"
            FRONTEND_TAGS="${FRONTEND_TAGS},${FRONTEND_BASE_TAG_PREFIX}:latest"
            echo "Adding 'latest' tag for release."
          elif [[ "$(Build.SourceBranchName)" == "main" ]]; then
            API_TAGS="${API_TAGS},${API_BASE_TAG_PREFIX}:latest-main"
            FRONTEND_TAGS="${FRONTEND_TAGS},${FRONTEND_BASE_TAG_PREFIX}:latest-main"
            echo "Main branch build, adding 'latest-main' tag."
          fi

          echo "Generated API Tags (Final for Docker@2): $API_TAGS"
          echo "Generated Frontend Tags (Final for Docker@2): $FRONTEND_TAGS"

          # Set output variables for subsequent steps (e.g., Docker@2 task)
          # Note: These output variables might still be masked in logs, but the value should be correct.
          echo "##vso[task.setvariable variable=apiImageTags;isOutput=true]$API_TAGS"
          echo "##vso[task.setvariable variable=frontendImageTags;isOutput=true]$FRONTEND_TAGS"
      displayName: 'Generate Docker Image Tags'
      name: generate_tags # This name is crucial for accessing output variables later

    # Debug Step: This will show the final comma-separated string that Docker@2 task receives
    - script: |
        echo "--- Debugging Generated Tags ---"
        echo "API_IMAGE_NAME: $(API_IMAGE_NAME)"
        # Note: ACR_REGISTRY_PREFIX/SUFFIX are pipeline variables, not script variables.
        # They will be expanded by Azure DevOps before the script runs.
        # The FULL_AZURE_CONTAINER_REGISTRY is a script variable.
        echo "Registry Prefix: $(ACR_REGISTRY_PREFIX)"
        echo "Registry Suffix: $(ACR_REGISTRY_SUFFIX)"
        echo "build.SourceBranchName: $(Build.SourceBranchName)"
        echo "build.SourceVersion: $(Build.SourceVersion)"
        echo "Final API Tags being passed to Docker task (should be comma-separated, unmasked value used): $(generate_tags.apiImageTags)"
        echo "Final Frontend Tags being passed to Docker task (should be comma-separated, unmasked value used): $(generate_tags.frontendImageTags)"
      displayName: 'Debug Generated Tags (Post-Generation Script)'

    - task: Docker@2
      displayName: 'Build and Push API Image'
      inputs:
        command: 'buildAndPush'
        repository: '$(API_IMAGE_NAME)'
        dockerfile: '$(Build.SourcesDirectory)/src/BjjEire.Api/Dockerfile'
        containerRegistry: '$(AZURE_CONTAINER_REGISTRY_SERVICE_CONNECTION)'
        tags: $(generate_tags.apiImageTags)
        buildContext: '.'
        # cacheFrom: 'youracrname.azurecr.io/bjjeire-api:latest' # Example for pulling cache from an existing image

    - task: Docker@2
      displayName: 'Build and Push Frontend Image'
      inputs:
        command: 'buildAndPush'
        repository: '$(FRONTEND_IMAGE_NAME)'
        dockerfile: '$(Build.SourcesDirectory)/src/bjjeire-app/Dockerfile'
        containerRegistry: '$(AZURE_CONTAINER_REGISTRY_SERVICE_CONNECTION)'
        tags: $(generate_tags.frontendImageTags)
        buildContext: '.'
        # cacheFrom: 'youracrname.azurecr.io/bjjeire-frontend:latest' # Example for pulling cache from an existing image

    # - task: AzureCLI@2
    #   displayName: 'Run ACR Vulnerability Scan (Optional but Recommended)'
    #   inputs:
    #     azureSubscription: 'AzureServiceConnectionWIF'
    #     scriptType: 'bash'
    #     scriptLocation: 'inlineScript'
    #     inlineScript: |
    #         # Example: Triggering a Defender for Cloud scan on a specific image
    #         # Replace with actual scan command/tool (e.g., Trivy, Azure Defender for Cloud)
    #         # This is a placeholder, as actual scanning depends on your chosen tool and setup.
    #         echo "Triggering vulnerability scan for $(AZURE_CONTAINER_REGISTRY)/$(API_IMAGE_NAME):$(Build.SourceVersion)"
    #         # az acr scan --registry $(AZURE_CONTAINER_REGISTRY_SHORT_NAME) --image $(API_IMAGE_NAME):$(Build.SourceVersion)
            
    #         echo "Triggering vulnerability scan for $(AZURE_CONTAINER_REGISTRY)/$(FRONTEND_IMAGE_NAME):$(Build.SourceVersion)"
    #         # az acr scan --registry $(AZURE_CONTAINER_REGISTRY_SHORT_NAME) --image $(FRONTEND_IMAGE_NAME):$(Build.SourceVersion)
            
    #         # For integrated Azure Defender for Cloud, scans happen automatically on push
    #         # but you might want to query results or integrate with other tools.

    # - script: |
    #     echo "Logging out from Docker registry"
    #     docker logout $(AZURE_CONTAINER_REGISTRY)
    #   displayName: 'Docker Logout (Good Practice)'
    #   condition: always()