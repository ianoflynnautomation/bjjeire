name: 'Test React Application (Integration)'
description: 'Installs dependencies and runs Vitest integration tests (jsdom + real query/router wiring)'
inputs:
  node-version:
    description: 'Node.js version'
    required: true
  working-directory:
    description: 'Directory of the React application (where package.json is)'
    required: true
runs:
  using: "composite"
  steps:
    - name: Setup Node.js and Cache
      uses: ./.github/actions/setup-node
      with:
        node-version: ${{ inputs.node-version }}
        working-directory: ${{ inputs.working-directory }}

    - name: Install Dependencies
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: npm ci

    - name: Run Integration Tests
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: npm run test:integration
