name: Cleanup Artifacts

on:
  schedule:
    - cron: "0 3 * * 0" # Every Sunday at 03:00 UTC
  workflow_dispatch:
    inputs:
      retention_days:
        description: "Delete workflow runs older than this many days"
        required: false
        default: "30"
      dry_run:
        description: "Dry run — print what would be deleted without deleting"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

permissions:
  contents: read

jobs:
  cleanup_artifacts:
    name: Cleanup Artifacts
    permissions:
      actions: write
    uses: ianoflynnautomation/bjjeire-ci-templates/.github/workflows/cleanup-artifacts.yml@2162415849bfd26a2a9284487c21425626e088a8  # v1.6.0
    with:
      retention-days: ${{ fromJSON(github.event.inputs.retention_days || '30') }}
      dry-run: ${{ (github.event.inputs.dry_run || 'false') == 'true' }}
