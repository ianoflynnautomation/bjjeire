; @rolldown/plugin-babel (React Compiler integration for rolldown-vite) declares
; a `rolldown` peer via the workspace: protocol that a standalone npm install
; cannot resolve. This keeps peer resolution lenient so `npm install` works the
; same as the lockfile-driven `npm ci` used in Docker/CI.
legacy-peer-deps=true
